import React from 'react'
import  {Box, CardContent, CradMedia, Typography} from '@mui/material';
import { CheckCircle } from '@mui/icons-material';
//  Link componant is used to create navigational links.
//like dont relode whole page like html<a> tag just reload 
//the url in the browser address bar without reading the entire page 
import {Link} from 'react-router-dom';
import {demoProfilePicture} from '../utils/constants';

const ChannelCard = () => {
  return (
    <div>
      ChannelCard
    </div>
  )
}

export default ChannelCard;
